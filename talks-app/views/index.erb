<%= glorify @config.description %>

<%= erb(:pagination, :layout => false, :locals => {
  :page => page,
  :num_pages => num_pages
}) %>

<% for talk in talks %>
  <%= erb(:talk, :layout => false, :locals => {
    :talk => talk,
    :is_section => false
  }) %> 
<% end %>

<%= erb(:pagination, :layout => false, :locals => {
  :page => page,
  :num_pages => num_pages
}) %>
